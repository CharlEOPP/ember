#include "ember/ecs/World.h"

// World is currently header-only (templated API). This translation unit exists so
// the ember_ecs static library has a compiled object and as a home for any future
// non-template helpers.

namespace ember {
} // namespace ember
